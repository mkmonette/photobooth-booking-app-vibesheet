# Booleans are strings; parse explicitly in code: const dark = process.env.REACT_APP_ENABLE_DARK_MODE === 'true'
REACT_APP_ENABLE_DARK_MODE=true

# LocalStorage keys (versioned to allow migrations)
REACT_APP_STORAGE_KEY=photobooth_app_v2
REACT_APP_STORAGE_BOOKINGS_KEY=photobooth_bookings_v2
REACT_APP_STORAGE_SETTINGS_KEY=photobooth_settings_v2
REACT_APP_STORAGE_USERS_KEY=photobooth_users_v2

# Booking rules & formats
# Numeric values are strings in env; parse with parseInt(value, 10) or Number(value)
REACT_APP_MAX_BOOKING_DAYS_AHEAD=180
REACT_APP_MIN_BOOKING_DURATION_MIN=30
REACT_APP_MAX_BOOKING_DURATION_MIN=240
REACT_APP_TIMEZONE=UTC
# Date/time format tokens: Moment-style by default (adjust in code if using another library)
REACT_APP_DATE_FORMAT=YYYY-MM-DD
REACT_APP_TIME_FORMAT=HH:mm

# App behavior flags (parse booleans explicitly)
REACT_APP_DISABLE_REMOTE_SYNC=true
REACT_APP_ALLOW_TEST_DATA=true
REACT_APP_STRICT_MODE=true
REACT_APP_ENABLE_ANALYTICS=false

# Network / hosting (frontend-only; safe defaults for local development)
REACT_APP_API_URL=http://localhost:3000
REACT_APP_PUBLIC_URL=/

# Logging / monitoring (leave sensitive values blank in repo)
REACT_APP_LOG_LEVEL=info
REACT_APP_SENTRY_DSN=""

# Guidance:
# - Do not put API secrets, DSNs, or private keys in this file in the repo.
# - Keep only a committed example (.env or component3.env as example); store real values in .env.local or CI.
# - Validate and convert env values early (app bootstrap) and fail fast on invalid/missing required configs.